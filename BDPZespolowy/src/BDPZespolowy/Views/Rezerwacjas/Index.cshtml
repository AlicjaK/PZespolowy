@model IEnumerable<BDPZespolowy.Models.Rezerwacja>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Rezerwacja_NrRezerwacji_)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rezerwacja_OstatKwota)
        </th>
        <th></th>
    </tr>
    
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Rezerwacja_NrRezerwacji_)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rezerwacja_OstatKwota)
        </td>
        <td>
            <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
        </td>
    </tr>
}
</table>
